
<div class="tech nugget">

  <a data-close></a>

  <form>
    <h3>Report an issue</h3>
    <div>
      <span class="legend">URL</span>
      <a href="{{currentUrl}}" target="_blank">{{currentUrl}}</a>
      <input type="hidden" name="URL" value="{{currentUrl}}" />
    </div>
    <div>
      <span class="legend">Screen</span>
      <span>{{currentScreen}}</span>
      <input type="hidden" name="Screen" value="{{currentScreen}}" />
    </div>
    <div>
      <span class="legend">Category</span>
      <select name="Category">
        <option value="">Select:</option>
        <option value="Bad data structure">The way data is related is wrong</option>
        <option value="Missing data">New properties are needed</option>
        <option value="Illogical">The application&quot;s behaviour illogical</option>
        <option value="Bug">The screen&quot;s is broken or gives error(s)</option>
        <option value="Usability">Interface is confusing, muddled, or hard to navigate</option>
        <option value="Presentation">Readability is poor or important details are hard to spot</option>
        <option value="Feature request">A new feature is needed</option>
      </select>
    </div>
    <div>
      <span class="legend">Urgency</span>
      <select name="Urgency">
        <option value="">Select:</option>
        <option value="1 - Launch critical">Launch critical</option>
        <option value="2 - Launch sub-optimal">Ideally before launch but possibly after</option>
        <option value="3 - Post-launch priority 1">Urgent</option>
        <option value="4 - Post-launch priority 2">Important</option>
        <option value="5 - Post-launch priority 3">Minor</option>
        <option value="6 - As and when">Quibble</option>
      </select>
    </div>
    <div>
      <span class="legend">Description</span>
      <textarea name="comments"></textarea>
    </div>
  </form>

  <h3>Screen = {{currentScreen}}</h3>

  <h4>Inserts</h4>
  <ul>
  {{#each currentInserts}}
    <li>{{this}}</li>
  {{else}}
    No inserted templates
  {{/each}}
  </ul>

  <h4>Parameters</h4>
  <ul>
  {{#each parameters}}
    <li>{{@key}} = {{this}}</li>
  {{else}}
    <div>No parameters set</div>
  {{/each}}
  </ul>

  <h4>Templates ( visited / <span class="unvisited">unvisited</span> )</h4>
  <ul>
  {{#each currentTemplates}}
    {{#if visited}}
    <li>{{@key}}</li>
    {{else}}
    <li><espan class="unvisited">{{@key}}</span></li>
    {{/if}}
  {{/each}}
  </ul>

  <h4>Current data:</h4>
  <pre>
  {{#each this}}
      {{@key}} => {{this}}
  {{#hasProperties this}}
  {{#each this}}
          {{@key}} => {{this}}
  {{#hasProperties this}}
  {{#each this}}
              {{@key}} => {{this}}
  {{#hasProperties this}}
  {{#each this}}
                  {{@key}} => {{this}}
  {{#hasProperties this}}
  {{#each this}}
                      {{@key}} => {{this}}
  {{#hasProperties this}}
  {{#each this}}
                        {{@key}} => {{this}}
  {{#hasProperties this}}
  {{#each this}}
                            {{@key}} => {{this}}
  {{#hasProperties this}}
  {{#each this}}
                                {{@key}} => {{this}}
  {{#hasProperties this}}
  {{#each this}}
                                      {{@key}} => {{this}}
  {{/each}}
  {{/hasProperties}}
  {{/each}}
  {{/hasProperties}}
  {{/each}}
  {{/hasProperties}}
  {{/each}}
  {{/hasProperties}}
  {{/each}}
  {{/hasProperties}}
  {{/each}}
  {{/hasProperties}}
  {{/each}}
  {{/hasProperties}}
  {{/each}}
  {{/hasProperties}}
  {{/each}}
  </pre>

</div>